'use strict';

/**
 * @ngdoc function
 * @name nextbook20App.controller:ColaboradoresCtrl
 * @description
 * # ColaboradoresCtrl
 * Controller of the nextbook20App
 */
	var app = angular.module('nextbook20App')
		app.controller('ColaboradoresCtrl', function () {
		    this.awesomeThings = [
		      'HTML5 Boilerplate',
		      'AngularJS',
		      'Karma'
		    ];
		});

		app.controller('col_nuevo_Ctrl', function () {
		    this.awesomeThings = [
		      'HTML5 Boilerplate',
		      'AngularJS',
		      'Karma'
		    ];
		  });
