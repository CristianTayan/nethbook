'use strict';

/**
 * @ngdoc service
 * @name nextbook20App.urlserver
 * @description
 * # urlserver
 * Service in the nextbook20App.
 */
angular.module('nextbook20App')
  .service('urlserver', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
